describe('METHOD: loadLocation should', () => {
  it('return cached values if present', () => {})
  it('fetch new request if values are missing', () => {})
  it('cache weather data value', () => {})
})

describe('METHOD: setLocation should', () => {
  it('set coordinates', () => {})
  it('trigger loadLocation', () => {})
})